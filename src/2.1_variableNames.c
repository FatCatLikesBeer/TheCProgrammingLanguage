#include <float.h>
#include <limits.h>
#include <stdio.h>

// Write a program to determing the ranges of:
//   (char, short, int, long) both signed and unsinged
int main(void) {
  printf("Max Char range: %d\n", CHAR_MAX);
  return 0;
}
